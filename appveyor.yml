platform:
  - x86
  - x64

branches:
  only:
    - master

environment:
  GOPATH: '%BuildFolder%'

install:
  - echo %PATH%
  - echo %GOPATH%
  - go env
  - go version
  - go get -v -t ./...

test_script:
  - go test -v ./...

build_script:
  - go build -v -o coduno.exe
